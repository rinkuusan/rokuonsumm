<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- マイク録音 -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <!-- FGS基本 + マイクタイプ -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MICROPHONE" />
    <!-- 通知 (Android 13+) -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!-- Doze対策 -->
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />
    <!-- Groq / LLM API -->
    <uses-permission android:name="android.permission.INTERNET" />
    <!-- 電話着信検知 -->
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <!-- 再起動後の通知 -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <application
        android:name=".App"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Rokuonsumm"
        android:networkSecurityConfig="@xml/network_security_config">

        <activity
            android:name=".ui.MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:theme="@style/Theme.Rokuonsumm.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!--
            foregroundServiceType="microphone" は API 30 以降でマイクを
            FGS から使うために必須。
        -->
        <service
            android:name=".recording.RecordingService"
            android:exported="false"
            android:foregroundServiceType="microphone" />

        <!--
            BOOT_COMPLETED は API 29 以降 FGS をバックグラウンドから起動できない。
            代わりに「タップして再開」通知を表示する。
        -->
        <activity
            android:name=".ui.SettingsActivity"
            android:exported="false"
            android:theme="@style/Theme.Rokuonsumm.NoActionBar"
            android:parentActivityName=".ui.MainActivity" />

        <activity
            android:name=".ui.AudioListActivity"
            android:exported="false"
            android:theme="@style/Theme.Rokuonsumm.NoActionBar"
            android:parentActivityName=".ui.MainActivity" />

        <activity
            android:name=".ui.DayTimelineActivity"
            android:exported="false"
            android:theme="@style/Theme.Rokuonsumm.NoActionBar"
            android:parentActivityName=".ui.MainActivity" />

        <activity
            android:name=".ui.UtteranceDetailActivity"
            android:exported="false"
            android:theme="@style/Theme.Rokuonsumm.NoActionBar"
            android:parentActivityName=".ui.DayTimelineActivity" />

        <activity
            android:name=".ui.StorageActivity"
            android:exported="false"
            android:theme="@style/Theme.Rokuonsumm.NoActionBar"
            android:parentActivityName=".ui.SettingsActivity" />

        <activity
            android:name=".ui.SearchActivity"
            android:exported="false"
            android:theme="@style/Theme.Rokuonsumm.NoActionBar"
            android:parentActivityName=".ui.MainActivity"
            android:windowSoftInputMode="adjustResize" />

        <activity
            android:name=".ui.SpeakerRegistrationActivity"
            android:exported="false"
            android:theme="@style/Theme.Rokuonsumm.NoActionBar"
            android:parentActivityName=".ui.SettingsActivity" />

        <receiver
            android:name=".BootReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
            </intent-filter>
        </receiver>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

    </application>

</manifest>
